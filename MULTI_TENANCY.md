# Enterprise RAG 多租户与权限模型

适用版本：`1.0.0-rc.1`。

## 隔离原则

`tenant_id` 是服务端数据边界，不是前端筛选条件。当前请求主体由 JWT 的 `sub` 和 `tenant_id` 定位；后端重新读取用户与租户，验证二者仍启用且 token tenant claim 与数据库一致，再计算角色权限。

`is_superuser` 只表示拥有**所属租户内**全部权限，不允许省略资源查询中的 `tenant_id`，也不具备平台级跨租户浏览能力。

## 租户归属

以下对象直接保存 `tenant_id`：

- 用户、角色、知识库；
- 文档、文档版本、入库任务、Chunk；
- 知识库重建任务；
- 会话、消息；
- 审计记录。

文档、版本、任务和 Chunk 还必须与同租户知识库/文档关系一致。向量 payload 保存 tenant、知识库、文档与版本标识；检索后会回到数据库再次执行租户和活动版本过滤，不能仅相信 Qdrant payload。

用户邮箱在同一租户内唯一，不同租户可以使用相同邮箱。登录界面和 API 使用 `tenant_slug + email + password` 定位主体；为兼容旧客户端，省略 tenant slug 只在该邮箱全库唯一时允许登录，存在多个匹配时会拒绝而不会任取第一条。

## 权限码

| 权限 | 服务端能力 |
| --- | --- |
| `kb:read` | 枚举和读取本租户知识库、重建状态 |
| `kb:write` | 创建/更新知识库、发起模型或维度重建 |
| `kb:delete` | 删除知识库及其文档、向量和会话 |
| `doc:read` | 读取文档、版本、任务和原文 Chunk |
| `doc:write` | 文件/URL 入库、版本更新、取消/重试、重嵌入和对账 |
| `doc:delete` | 删除文档、版本 Chunk、向量和原文件 |
| `chat:use` | 混合检索、流式/非流式问答、读取会话和导出 Markdown |
| `conversation:delete` | 删除本租户会话 |
| `admin:manage` | 管理本租户用户、角色、权限和审计记录 |

新建普通用户默认获得成员角色；管理员角色必须保留全部权限。系统角色不能删除，管理员角色不能被降权。角色仍被用户引用时不能删除。

## API 判定顺序

1. 无 token、签名错误、过期或用户不存在：401。
2. 用户或租户停用、tenant claim 不一致：403。
3. 主体缺少目标权限：403。
4. 查询资源时同时带对象 ID 和当前 `tenant_id`；不属于当前租户或不存在：404。
5. 对文档/会话等子资源继续核对父知识库、文档和活动版本关系。

返回 404 的目的，是避免攻击者通过状态码判断另一个租户的对象 ID 是否存在。

## 管理保护

- 不能停用当前登录账号。
- 每个租户必须至少保留一名启用且拥有 `admin:manage` 的用户。
- 系统管理员角色不能删除、重命名或移除权限。
- 用户角色只能引用同一租户的角色。
- 审计查询固定在当前租户，并使用分页上限。

## 限流边界

有效 Bearer token 先解析 tenant claim，以租户作为 Redis 配额键；匿名或无效 token 按客户端 IP。Redis Lua 脚本保证多个 backend 副本对同一窗口原子计数。生产模式强制 `redis + fail_closed`，避免 Redis 故障时每个副本各自放行。

## 回归证据

`backend/tests/test_tenant_rbac.py` 和相关 API 测试覆盖：

- superuser 不能枚举、读取、更新、删除另一个租户的知识库、文档、版本、任务、Chunk、会话、审计、用户或角色；
- 伪造或错配 JWT tenant claim 被拒绝；
- 同租户用户/角色 CRUD、未知权限、跨租户角色引用和最后管理员保护；
- 新增文档版本、重试、取消、对账等后续端点仍保持租户边界；
- 删除与检索回源不会通过 Qdrant payload 绕过数据库租户过滤。

固定评估集另有跨租户问题，要求隔离准确率为 1.0。

## 当前边界

当前没有平台级跨租户运营角色，也没有租户切换功能。需要平台运营能力时，应新增独立控制面、明确审计和显式租户选择，不能复用 `is_superuser` 绕过现有查询条件。
