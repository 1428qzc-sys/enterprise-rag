[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing:skip-covered --cov-fail-under=75
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning

[coverage:run]
omit =
    app/api/*
    app/security_network.py
    app/services/parsing.py
